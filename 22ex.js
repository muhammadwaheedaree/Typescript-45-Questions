//Intentional Error:
// Define an array
var fruits = ["Apple", "Banana", "Orange"];
// Intentional error: Access an index that doesn't exist
console.log(fruits[3]); // Error: Index out of bounds
// Correct the error by accessing an existing index
console.log(fruits[2]); // Accessing the existing index 2
// Display the corrected array
console.log(fruits);
